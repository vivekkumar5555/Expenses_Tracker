# Render Blueprint for SmartSpend+ Frontend
# Deploy as: Web Service (not Static Site)

services:
  - type: web
    name: smartspend-frontend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
